{
  "name": "ogsnap",
  "version": "1.0.0",
  "description": "Dynamic OG image generation API — one URL, perfect social cards",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/og": "^0.6.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0"
  }
}
